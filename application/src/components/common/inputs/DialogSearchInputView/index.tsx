import React, { memo } from 'react';
import { View } from 'react-native';

const DialogSearchInputView = () => {
  return (
    <View style={[]} />
  );
};

export default memo(DialogSearchInputView);
